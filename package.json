{
  "name": "agoda-toolkit-2",
  "version": "2.2.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "Agoda Toolkit",
    "manifest": "src/manifest.json",
    "main": "Agoda-Toolkit.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.16"
  },
  "author": "Agoda",
  "icon": "icons/icon.png",
  "dependencies": {
    "@skpm/fs": "^0.2.5",
    "mocha-js-delegate": "^0.1.1",
    "npm": "^6.9.0",
    "sketch-module-user-preferences": "^1.0.2",
    "sketch-module-web-view": "^1.2.3"
  }
}
